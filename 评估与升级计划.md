# 文件自动分类工具 V1.2 — 评估报告与升级计划

> 评估方式：通读全部源码（`文件自动分类工具.py`，约 760 行），并针对可疑点编写脚本实际运行核心逻辑验证（不启动 GUI）。
> 结合的工作场景：从多个单位查询多名人员信息，反馈以压缩包形式按**单位**打包返回，需要按**人员**归档，一人一个文件夹。

---

## 一、总体评价

V1.2 的定位准确、主流程完整：解压（含嵌套、含密码、含密码本自动尝试）→ 按文件名关键字分类 → 未命中兜底（身份证号识别 / 未分类文件夹）→ 失败归集，"绝不悄悄丢文件"的设计理念是对的。代码可读性好，后台线程 + 主线程弹框的处理正确，高分屏适配到位。

但实测发现了 **4 个已确认的缺陷**（其中 2 个会在日常使用中直接造成分类错误或解压失败），且针对"多单位反馈、按人归档"这一实际工作场景，目前缺少几项关键能力（人员多标识合并、来源单位追溯、反馈核对表）。

---

## 二、实测确认的问题（附验证结果）

### 🔴 P0-1 AES 加密 zip 全部解压失败（稳定性）

**实测**：用正确密码、且密码已在密码本候选中的 AES-256 加密 zip，解压结果为**失败**，被扔进"解压失败"文件夹。

原因：Python 标准库 `zipfile` 只支持老式 ZipCrypto 加密，**不支持 AES**。而 WinRAR、7-Zip、班迪压缩等工具给 zip 加密时经常默认或可选 AES-256。对方单位只要用这类工具加密码，本工具就必然失败，且日志只提示"密码错误/未提供正确密码"，会误导使用者反复输入正确密码却始终失败。

**修复**：改用 `pyzipper` 库（API 与 zipfile 几乎一致，同时兼容 ZipCrypto 和 AES），约 20 行改动。

### 🔴 P0-2 zip 路径穿越漏洞（安全/稳定性）

**实测**：构造含 `../../escaped.txt` 条目的 zip，解压后文件**成功逃出目标目录**。

本工具处理的是外来压缩包（从网上查询、外单位反馈获得），恶意或异常构造的压缩包可以把文件写到临时目录之外的任意可写位置，甚至覆盖已有文件。`_fix_zip_name` 返回的文件名直接 `os.path.join` 后未做归一化校验。

**修复**：解压前对每个条目做 `os.path.realpath` 归一化，确认仍在目标目录内，否则跳过并记日志；7z/rar 路径同样加校验。约 15 行改动。

### 🟡 P1-1 关键字前缀冲突导致归错人（正确性）

**实测**：关键字按 `张三`、`张三丰` 顺序输入时，文件 `张三丰_材料.txt` 被归入 **张三** 的文件夹。

当前按输入顺序取第一个命中，人名存在包含关系时（张三/张三丰、王伟/王伟民）必然归错，而且用户很难察觉。**修复**：匹配时按关键字长度降序优先（最长匹配优先），一行改动即可消除绝大多数误分。

### 🟡 P1-2 身份证号识别无校验（正确性）

**实测**：`订单号123456789012345678.txt` 中的任意 18 位数字被当作身份证号，会为其建文件夹。

文件名中的订单号、流水号、时间戳拼接数字都可能是 18 位。**修复**：按 GB 11643 做加权校验位验证（约 10 行），并可选支持 15 位老号码；地区码前 2 位限定在有效省份码范围可进一步降误判。

### 其余审查发现（未逐一实测，代码层面确认存在）

| 级别 | 问题 | 说明 |
| --- | --- | --- |
| 🟡 | Windows 长路径 | 嵌套解压路径层层加 `xxx.zip_解压`，中文长文件名极易超过 260 字符 MAX_PATH,导致写文件失败。需 `\\?\` 前缀或打包时声明 longPathAware |
| 🟡 | 分卷压缩包 | `.part1.rar/.part2.rar`、`.z01` 会被当成多个独立压缩包逐个解压，part2+ 必然失败进"解压失败"，让人困惑。应识别分卷、只从首卷解压 |
| 🟡 | 密码试错代价大 | rar/7z 每试一个候选密码就**整包解压一遍**，大包 + 几十个候选密码时极慢。应只解压其中最小的一个文件来验证密码 |
| 🟡 | 双倍磁盘 I/O | 先全部解压到临时目录，再逐个 `copy2` 到输出目录——每个文件写两遍。临时目录默认在 C 盘，大批量时可能撑爆系统盘。应把临时目录放到输出目录同盘，分类时用 `move` |
| 🟢 | 无取消按钮 | 任务一旦开始只能杀进程；也没有进度条（只有滚动日志） |
| 🟢 | 无日志文件 | 日志只在界面里，关窗即失；出问题无法事后追查 |
| 🟢 | ZipCrypto 解密慢 | 标准库纯 Python 解密，大加密包速度明显慢于 7z/WinRAR |

**性能小结**：对典型场景（几十个压缩包、几百个文件）当前性能可接受；瓶颈集中在"密码整包试错"和"双倍写盘"两处，都有低成本改法。分类环节按文件名字符串匹配，即使几百个关键字也毫无压力。

---

## 三、结合工作场景的功能差距分析

场景：向 5 个单位查询 10 个人 → 每个单位回一个包（或混在一个包里）→ 需要按人归档。当前版本能跑通基本流程，但有四个实际痛点没覆盖：

1. **一人多标识会"分家"**。A 单位反馈的文件名带姓名，B 单位带身份证号，C 单位写了曾用名——同一个人会散落在 2~3 个文件夹里。当前"关键字=文件夹"是一对一的。
2. **来源单位信息丢失**。文件归到人员文件夹后，看不出是哪个单位反馈的（除非文件名本身带单位名）。核查、补证、催办时都需要这个信息。
3. **无反馈核对**。"5 个单位 × 10 个人"共 50 个格子，哪些单位对哪些人没有反馈？现在只能人工数文件夹。
4. **文件名不含人名就没辙**。单位反馈 `回执001.pdf`、或者把 10 个人的信息放在一个 Excel 里时，只能进"未分类"。

---

## 四、升级计划

### V1.3 — 稳定性与正确性（优先，改动小、收益大）

| # | 内容 | 对应问题 |
| --- | --- | --- |
| 1 | 换用 pyzipper，支持 AES 加密 zip | P0-1 |
| 2 | 解压路径穿越防护（zip/7z/rar 统一校验） | P0-2 |
| 3 | 关键字最长匹配优先 | P1-1 |
| 4 | 身份证校验位验证 + 15 位老号支持 | P1-2 |
| 5 | 密码验证只解压单个最小文件 | 性能 |
| 6 | 临时目录放输出盘 + move 代替 copy | 性能/C盘 |
| 7 | 分卷压缩包识别（只从首卷解压） | 稳定性 |
| 8 | 长路径支持（`\\?\` 前缀） | 稳定性 |
| 9 | 进度条 + 取消按钮；日志同步写入输出目录 `分类日志.txt` | 易用性 |

### V1.4 — 易用性与工作流（围绕"多单位查多人"场景）

| # | 内容 | 说明 |
| --- | --- | --- |
| 1 | **人员名单管理** | 从 Excel 导入或直接粘贴"姓名 / 身份证号 / 曾用名 / 备注"，一人一行多列；任一标识命中都归入同一文件夹（文件夹名建议"姓名+证号后4位"，防同名人混淆）。这是把"关键字"升级为"人员档案"的核心改造 |
| 2 | **来源单位追溯** | 以顶层压缩包名（通常就是单位名）作为来源标记：可选在人员文件夹下按单位建子文件夹，或给文件名加"【某单位】"前缀 |
| 3 | **反馈核对表** | 分类完成后自动生成 人员×单位 矩阵（Excel/CSV）：每格填该单位反馈了几个文件，空格即"未反馈"，直接当催办清单用 |
| 4 | 拖拽添加压缩包/文件夹（tkinterdnd2） | 免去逐层点选 |
| 5 | 配置记忆 | 输出目录、选项、上次人员名单自动保存（JSON），支持保存/加载"任务模板"（同一批人查多轮很常见） |
| 6 | 试运行（预览）模式 | 先展示"哪个文件 → 哪个文件夹"的清单再执行，避免大批量分错后返工 |

### V2.0 — 能力扩展

| # | 内容 | 说明 |
| --- | --- | --- |
| 1 | **内容级匹配** | 文件名未命中时，读取文件内容找姓名/证号：Excel(openpyxl)、Word(python-docx)、PDF 文本层(pdfplumber)。可将"未分类"比例大幅压低 |
| 2 | **Excel 按人拆分** | 单位把多人信息放一个 Excel 时，按身份证号/姓名列拆成每人一个文件分别归档（保留原表一份于"原始反馈"） |
| 3 | 跨单位重复文件去重（按内容哈希，可选） | 多单位反馈同一份材料时避免重复占用 |
| 4 | 更多格式：tar/gz、zipx；扫描件 OCR 识别（可选、后期） | OCR 依赖较重，建议作为可选插件 |
| 5 | 命令行模式 | 支持无界面批处理，可配合计划任务自动处理固定收件目录 |

### 界面翻新（可与 V1.4 并行）

- 低成本方案（推荐先做）：引入 **ttkbootstrap**，一行主题设置即可让 tkinter 界面现代化，原有代码几乎不动；补充进度条、结果树形预览（人员文件夹 → 文件列表）、统计摘要面板。
- 长期方案：若 V2.0 功能（预览表格、Excel 拆分配置）复杂化，再考虑迁移 PySide6；现阶段不建议，tkinter 打包体积小、兼容性好，符合"免安装单 exe"的分发方式。

### 工程化建议

- 单文件已近 800 行,建议按 `extract.py / classify.py / password.py / gui.py` 拆分模块，核心逻辑与 GUI 解耦；
- 为 `classify_file`、`extract_archive`、身份证校验、密码本解析补充单元测试（本次评估用的验证脚本可直接演化为测试用例）；
- 涉及公民个人信息处理，保持"全程本地、不联网"的现有设计，并在 README 中明确声明；日志中继续避免记录密码明文（当前做法正确，保持）。

---

## 五、优先级建议

1. **立即做**（半天~两天）：AES 支持、路径穿越防护、最长匹配、身份证校验 —— 前两个是"用着用着必然踩坑"的问题，后两个直接决定分类准确率。
2. **下一版做**：人员名单（多标识合并）+ 来源单位 + 核对表 —— 对实际工作提效最大的三件事。
3. **之后做**：内容级匹配与 Excel 拆分 —— 收益大但工作量和依赖也大，等 V1.4 稳定后再上。
