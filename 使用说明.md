# 文件自动分类工具 — 使用说明

一个带界面的 Windows 小工具:自动解压压缩包,再按**文件名**里的
人名 / 公司名 / 身份证号等关键字,把文件分到不同文件夹。
只看文件名、不读文件内部内容,所以又快又稳,Excel / Word / PDF 一视同仁。

---

## 一、怎么运行(三步)

**第 1 步:装 Python(只需装一次)**

到 https://www.python.org/downloads/ 下载 Python 3.8 以上版本。
安装时**务必勾选** “Add Python to PATH”(添加到环境变量)这一项。
（Windows 版 Python 自带界面库 tkinter,无需额外安装。)

**第 2 步:装解压 7z / rar 的支持(可选)**

工具默认就能处理 **zip**。如果你的压缩包还有 7z 或 rar,按需安装:

打开“命令提示符”(开始菜单搜 cmd),粘贴运行:

    pip install py7zr        # 支持 .7z
    pip install rarfile      # 支持 .rar

其中 rar 还需要电脑装了 WinRAR 或 unrar,一般装了 WinRAR 就行。
不装也没关系,工具会自动跳过对应格式并在界面上提示。

**第 3 步:运行**

双击 `文件自动分类工具.py` 即可打开界面。
（若双击没反应,右键 → “打开方式” → 选 Python。)

---

## 二、界面怎么用

1. **第一步:添加压缩包** —— 可一次选多个压缩包,或直接选一个装满压缩包的文件夹(会自动找里面所有 zip/7z/rar,含嵌套压缩包)。
2. **第二步:选输出目录** —— 分类结果会保存到这里。
3. **第三步:输入关键字** —— 每行写一个,比如 20 个人就写 20 行。以 `#` 开头的行会被忽略。
4. **第四步:选项**
   - 一个文件命中多个关键字时:
     - “只归入第一个”:按你输入的先后顺序,归到第一个命中的关键字文件夹。
     - “复制到每个文件夹”:该文件在每个命中的关键字文件夹里各放一份。
   - “自动识别身份证号”:勾选后,**没命中任何关键字**的文件,会去文件名里找 18 位身份证号,按号码自动建文件夹归类。
5. 点 **开始分类**。底部日志会实时显示进度,完成后可点“打开结果文件夹”查看。

> 未命中任何关键字、也没识别到身份证号的文件,会统一放进 **“未分类”** 文件夹,方便你人工复核,绝不丢文件。

---

## 三、几个实用说明

- **原文件不动**:工具是“复制”到结果目录,不会删改你的原始压缩包。
- **同名不覆盖**:不同压缩包里有同名文件时,自动加 `(1)(2)` 区分。
- **中文文件名乱码**:已内置修复,老式 zip 的中文名也能正常识别。
- **身份证号识别**:识别 18 位号码(17 位数字 + 末位数字或 X),这是最可靠的分类依据,建议尽量勾上。

---

## 四、想做成免安装的 .exe(可选,进阶)

如果不想让每台电脑都装 Python,可以把它打包成一个 exe,双击即用。
在装好 Python 的电脑上,命令提示符里运行:

    pip install pyinstaller
    pyinstaller --onefile --windowed --name 文件自动分类工具 文件自动分类工具.py

打包完成后,在生成的 `dist` 文件夹里能找到 `文件自动分类工具.exe`,
拷到其他 Windows 电脑上双击就能用(无需再装 Python)。
注意:要支持 7z / rar,打包前需先 `pip install py7zr rarfile`。
