# 文件自动分类工具 V4.1 — 使用说明

一个带界面的 Windows 小工具,有两种工作模式:

> **按人归档** —— 向 5 个单位查询 10 个人的信息,各单位用压缩包反馈回来:
> 工具自动解压所有压缩包,把文件按"一人一个文件夹"归好,
> 标注每份材料来自哪个单位,并生成一张核对表:哪个单位没反馈哪个人,一眼看清。
>
> **文件整理** —— 下载目录、移动硬盘里积攒多年的杂乱文件:
> 按类型+时间整理好,顺手挑出重复文件、旧版本和垃圾文件,不满意可一键撤销。
> (见文末第六节)

---

## 一、怎么运行(三步)

**第 1 步:装 Python(只需装一次)**

到 https://www.python.org/downloads/ 下载 Python 3.8 以上版本。
安装时**务必勾选** "Add Python to PATH"(添加到环境变量)这一项。

**第 2 步:装依赖(推荐全装,缺了对应功能会自动关闭)**

打开"命令提示符"(开始菜单搜 cmd),在工具所在目录运行:

    pip install -r requirements.txt

不想全装的话,最重要的是这三个:

    pip install PySide6       # 界面框架(必装)
    pip install pyzipper      # 加密 zip(AES)支持,强烈建议
    pip install py7zr         # .7z 支持
    pip install openpyxl      # Excel 名单导入 / 内容匹配 / 核对表

**第 3 步:运行**

双击 `文件自动分类工具.py` 即可打开界面。
(若双击没反应,右键 → "打开方式" → 选 Python。)

---

## 二、界面怎么用

1. **第一步:添加压缩包** —— 可一次选多个压缩包,或直接选一个装满压缩包的文件夹;也可以直接把文件拖进窗口。
2. **第二步:选输出目录** —— 分类结果、核对表、日志都保存到这里。
3. **人员名单页** —— 每行写一个人,格式:

       姓名,身份证号,曾用名

   后两项可以省略;分隔符用逗号或空格都行;以 `#` 开头的行是注释。
   **同一个人的姓名、证号、曾用名,任何一个出现在文件名里,文件都会归进 TA 的文件夹** —— 所以 A 单位按姓名命名、B 单位按证号命名也不会分家。

   人多的话点 **"从 Excel 导入名单"**:表格里放"姓名 / 身份证号 / 曾用名"三列(有没有表头都行),一键导入。

   下次还查同一批人?点 **"保存为模板"**,以后一键加载。

4. **分类选项页**
   - 一个文件命中多个人:只归最匹配的一个,或每人复制一份。
   - 自动识别身份证号:没命中名单的文件,按文件名里的身份证号归类(18 位号码带校验,订单号等长数字不会误判)。
   - **来源单位标注**:来源单位 = 压缩包的文件名。推荐"按单位建子文件夹",归档后长这样:

         结果目录/
           张三/
             公安局反馈/张三_户籍.pdf
             银行反馈/110101...851X_流水.xlsx
           李四/
             ...

   - **试运行预览**:勾上后,先弹出"哪个文件→哪个文件夹"的完整清单,点"确认执行"才真正动文件。第一次用或者文件很多时建议勾上。
     **发现分错的不用等执行完再挪**:在预览里双击"归入文件夹"那一格就能改;按住 Ctrl/Shift 选多行,点"改归到…"可以批量改。

5. **高级功能页**
   - **内容匹配**:文件名看不出是谁的(比如"回执001.pdf"),就打开文件在内容里找名单中的姓名/证号。支持 Excel、Word、PDF、txt,**包括旧版 .doc / .xls**(Office 97-2003 格式,单位反馈里很常见)。名单里没有的人,智能识别发现的姓名和内容里的身份证号也能兜底归档。
   - **Excel 按人拆分**:单位把 10 个人的信息放在同一张表里反馈?勾上后按行拆成每人一份,原表保留在"原始反馈"文件夹备查。
   - **内容去重**:两个单位反馈了同一份文件时只留一份。
   - 密码本:遇到加密包,自动尝试输入文件夹里 txt 中的密码,失败才弹框问你;密码输对一次会记住,后面的包自动复用。
   - 删除原包:默认关闭;开启后只删【成功解压】的,且是移入回收站。

6. 点 **开始分类**。有进度条,随时可点"取消"。完成后:
   - 每人一个文件夹,按单位放好;
   - **反馈核对表.xlsx / .csv**:行是人、列是单位、格子是文件数,空格标黄 = 该单位没反馈这个人,可直接当催办清单;
   - **分类日志_时间.txt**:全过程记录,出问题可回查。

> 未命中名单、也没识别到身份证号的文件,统一放进 **"未分类"** 文件夹(同样按单位分子文件夹),方便人工复核,绝不丢文件。

---

## 三、几个实用说明

- **原文件不动**:工具把结果写到输出目录,不会删改你的原始压缩包(除非你主动勾选删除)。
- **同名不覆盖**:同名文件自动加 `(1)(2)` 区分。
- **加密 zip**:V2.0 起支持 AES 加密的 zip(WinRAR/7-Zip 加密码常用这种),需要装 pyzipper。
- **分卷压缩包**:`xxx.part1.rar`、`xxx.7z.001` 直接选首卷即可(其余卷放在同一目录);`.zip.001` 暂不支持,请先用 7-Zip 合并。
- **中文文件名乱码**:已内置修复,老式 zip 的中文名也能正常识别。
- **同名的两个人**:名单里有两个"王伟"时,文件夹自动带证号后 4 位区分(如 `王伟_851X`)。
- **隐私**:全程本地运行、不联网;密码不写入日志。

---

## 四、命令行 / 定时自动处理(进阶)

带参数运行即为无界面模式,适合配合 Windows"任务计划程序"定时处理固定收件目录:

    python 文件自动分类工具.py -i D:\收件 -o D:\结果 --roster 名单.xlsx --content-match

全部参数看:

    python 文件自动分类工具.py -h

---

## 五、想做成免安装的 .exe(可选,进阶)

见 `README.md` 中的 PyInstaller 打包命令;生成的 exe 拷到其他 Windows 电脑双击就能用(无需装 Python 或 WinRAR)。

---

## 六、文件整理模式

除了"按人归档",这个工具还能帮你整理**平时积攒的各种文件** —— 下载目录、
从移动硬盘拷回来的一堆东西、多年攒下的工作学习资料。

### 怎么用

1. 界面第②区把 **工作模式** 切到「文件整理(下载目录 / 移动硬盘)」。
2. 第①区选要整理的文件夹(可多选),第②区选整理结果放哪里。
   - 想**原地整理**,把结果目录也填成同一个文件夹即可。
3. 切到 **📁 文件整理** 页,选目录布局:
   - `{类别}/{年}` —— 先按类型再按年份(推荐,找特定类型的东西最快)
   - `{年}/{类别}` —— 先按年份再按类型(适合回忆"那年做过什么")
   - `{类别}/{年月}` —— 文件特别多时用
   - `{来源}/{类别}` —— 微信 / 截图 / 相机照片分开放
   - `{原目录}/{类别}` —— 保留原来的文件夹名(项目/课程资料适用)
   - `{类别}/{大小档}` —— 类型 → 大小档(找占空间的东西)
   - 也可以自己编辑组合,占位符有 `{类别} {年} {年月} {来源} {扩展名} {原目录} {大小档}`
4. 点 **开始整理**。默认会先给你看清单(可双击改归属),确认后才动文件。

### 按"真实日期"归类(而不是修改时间)

照片从手机/相机拷到硬盘后,修改时间往往变成拷贝当天;下载的文件修改时间是下载时间。
所以按年份归类时,工具会**优先用文件的真实日期**:

1. 照片的 **EXIF 拍摄时间**(最准)
2. **文件名里的日期** —— `IMG_20180712_153000.jpg`、`微信图片_20230815120000.jpg`、
   `2021-03-08工作总结.docx` 这类命名本身就带日期
3. 都没有才用**文件修改时间**

日志里会告诉你这批文件各用了哪种依据。如果你就想按修改时间分,
把「按真实日期归类」这个勾去掉即可。

> 身份证号、订单号里的数字不会被误当成日期。

### 它会顺手帮你做的"文件体检"

| 挑出来的 | 放到哪 | 说明 |
| --- | --- | --- |
| 重复文件 | 「重复文件」 | 内容完全相同的只留一份(优先留原件),其余集中放好,并告诉你删掉能省多少空间 |
| 旧版本 | 「旧版本」 | `报告(1)`、`报告 - 副本`、`方案-最终版2` 这类,最新的留在正常分类里,旧的收进来 |
| 垃圾文件 | 「可清理」 | Thumbs.db、`~$` 临时文件、没下载完的残骸、空文件,按原因分好 |
| 大文件(可选) | 「大文件」 | 超过设定阈值的单独归集,方便腾空间 |

整理完还会生成 **整理报告.xlsx**:总共多少文件、各类型占多少空间、
哪年的文件最多、最大的 20 个文件是什么 —— 看一眼就知道硬盘里到底装了什么。

### 先体检、再决定要不要整理

第一次面对一块陌生硬盘,可以先勾上 **「只体检不整理」** 跑一遍:
**一个文件都不动**,只告诉你里面有多少重复、多少垃圾、多少旧版本、
各类型占多少空间。看完报告觉得值得整理,再去掉这个勾正式跑。

### 整理完一键清理(腾空间)

「可清理」「重复文件」「旧版本」这三个文件夹是给你**核对**用的,
默认不会自动删。核对完想腾空间,点 **「🗑 清理」** 按钮:

- 会先列出每个文件夹有多少文件、占多少空间,让你选择删哪些
- 删除是**移入回收站**(误删可从回收站还原)
- 只删这三个由工具归集出来的文件夹,正常分类的文件绝不碰

> 注意:被清理掉的文件,整理台账就无法再撤销了(但回收站里还有)。

### 不满意可以一键还原

整理默认是**移动**文件(否则整理硬盘要双倍空间)。为了让你敢用,
每一步操作都记进了「整理台账_时间.jsonl」:

> 在 **📁 文件整理** 页点 **「↩ 撤销上次整理」**,选择那次整理的台账,
> 所有文件会被移回整理前的原始位置。

即使整理到一半断电或被强制关掉,已完成的部分同样能撤销。
还原时如果原位置已经有了同名新文件,不会覆盖,会自动加序号。

### 顺带把压缩包也解开

勾上 **「顺带解开压缩包」**,zip/rar/7z 里的文件会被解出来一起整理,
按它所在的**压缩包名**归档(如 `2023年度资料/PDF/手册.pdf`);
压缩包本身仍会照常归到「压缩包」分类,不会丢。加密包会弹框问密码。

> 撤销时,解压出来的文件会被删掉、压缩包移回原位 —— 完全回到整理前的状态。

### 查"这个文件当初在哪"

每次整理都会记进 **整理历史.db**。点 **「🔍 查整理历史」** 输入文件名的一部分,
就能看到它历次整理前后的完整路径 —— 找不到东西时特别有用。

命令行:`python 文件自动分类工具.py -o D:\整理结果 --history 手册`

### 想按自己的方式分?

「🔧 高级功能」页的**高级分类规则**在整理模式下同样有效,而且**优先级高于类型布局**。
比如:

- `包含文字: 发票 → 财务票据`(文件名带"发票"的都进财务票据)
- `扩展名: psd → 设计稿`
- `正则匹配: ^2023.*合同 → 2023年合同`

这样就能在"按类型"之上叠加你自己的主题分类。

### 命令行(可配合计划任务定期整理)

    python 文件自动分类工具.py --mode organize -i D:\下载 -o D:\整理结果
    python 文件自动分类工具.py --mode organize -i D:\下载 -o D:\体检 --scan-only
    python 文件自动分类工具.py --mode organize -i D:\下载 -o D:\结果 --purge all
    python 文件自动分类工具.py --list-layouts
    python 文件自动分类工具.py --undo "D:\整理结果\整理台账_20240115_103000.jsonl"
